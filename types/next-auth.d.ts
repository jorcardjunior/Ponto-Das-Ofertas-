// Intentionally empty — all type augmentation is handled via SessionWithTenant in types/session.ts
// Module augmentation for next-auth v4 is not used because it conflicts with the library's re-export structure
